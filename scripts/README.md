# scripts

Spike and operational scripts.

- **`spike-pay-endpoint.ts`** — the W1–2 milestone: one agent pays one endpoint via the
  t54 facilitator, end to end, with a visible on-ledger txid. Run with
  `pnpm --filter @payper/scripts spike`.
